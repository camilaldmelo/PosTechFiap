apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-nuGETBurger-soat-svc-lb
spec:
  selector:
    app: tech-challenge-nuGETBurger-soat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
